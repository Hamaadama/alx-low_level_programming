#include <stdio.h>

/**
 * main - function main of C.
 *
 * Return: Always 0 (Succes).
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	printf("%d", number);
	printf("\n");

	return (0);
}
